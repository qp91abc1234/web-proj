/**
 * 事件总线事件类型定义
 *
 * 在这里定义所有需要使用的事件及其参数类型，确保类型安全
 *
 * @example
 * ```ts
 * // 定义事件
 * export type Events = {
 *   'user:login': { userId: string; username: string }
 *   'user:logout': void
 *   'theme:change': { theme: 'light' | 'dark' }
 * }
 * ```
 */
export type Events = {
  // 在这里添加事件类型定义...
}
